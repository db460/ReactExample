import { createContext } from "react";
export const UserContext = createContext(null);

export const UserDataContext = createContext(null);
export const SelectedNetworksContext = createContext(null);
export const UpdateUserDataContext = createContext(null);
export const UpdateSelectedNetworksContext = createContext(null);

export const AppDataContext = createContext(null);

